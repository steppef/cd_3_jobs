name: Deploy for stage server

on:
  push:
    branches:
      - master

jobs:
  git_tag:
#    env:
#      TAG_VERSION: ${{ github.event.repository.updated_at}}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set tag version as output
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - name: Set git tag
        run: git tag ${{ steps.date.outputs.date }}
      - name: push to remote
        run: git push origin ${{ steps.date.outputs.date }}

  build:
    needs: git_tag
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USER }}/cd_3_jobs:${{ steps.date.outputs.date }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
#      - name: install ssh keys
#        run: |
#          install -m 600 -D /dev/null ~/.ssh/id_rsa
#          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
#          ssh-keyscan -H ${{ secrets.SSH_HOST }} > ~/.ssh/known_hosts
#      - name: connect to server
#        run: ssh -t -t ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: cd ${{ secrets.WORK_DIR }} && docker-compose pull && docker-compose down && docker-compose up -d && exit
      - name: cleanup
        run: rm -rf ~/.ssh